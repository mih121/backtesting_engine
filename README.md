# backtesting_engine
The code represents core of backtesting using csv data into readable plot files using "what-if" analysis.

The execution_file shows how one Master strategy put together both buy and sell logic and executes them.
